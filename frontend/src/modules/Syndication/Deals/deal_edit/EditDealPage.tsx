/**
 * Edit an existing deal — same two-step wizard as {@link CreateDealPage}.
 * Canonical URL: `/deals/create?edit=<dealId>` (see `DealRowActions`, `DealPreviewModal`, `DealsListPage`).
 */
export { CreateDealPage as EditDealPage } from "../deal_create/CreateDealPage"
