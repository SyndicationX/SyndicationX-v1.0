/** Re-export so imports from `./components/DealEsignTemplatesTab` resolve (canonical module lives under `tabs/esign_templates/`). */
export {
  DealEsignTemplatesTab,
  ESIGN_ENTITY_CATEGORIES,
  ESIGN_FOLDER_SLUG,
  type EsignEntityCategory,
} from "../tabs/esign_templates/DealEsignTemplatesTab"
